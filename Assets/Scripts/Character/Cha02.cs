using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cha02 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Cha01 i = new Cha01("a");
        //i.TT(2);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
